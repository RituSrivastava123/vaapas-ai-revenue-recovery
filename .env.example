# Optional. If unset, the negotiation agent runs in SIMULATOR mode --
# a deterministic rule-based stand-in -- so the whole pipeline works
# out of the box with zero setup.
#
# If set, negotiation_agent.py calls the real Claude API for both the
# negotiation turn and the extraction pass.
ANTHROPIC_API_KEY=
